## R CMD check results

0 errors | 0 warnings | 1 note

* This is a new submission.

## Notes

* The NOTE about `future` in Suggests is expected: it is used conditionally
  in `race()` for optional parallel solver execution via `requireNamespace()`.

## Downstream dependencies

There are currently no downstream dependencies for this package.
